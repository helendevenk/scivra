/**
 * Stub for @/.source (Fumadocs generated source).
 * Used by vitest to avoid resolving MDX imports during testing.
 */
export const docs = [];
export const logs = [];
export const pages = [];
export const posts = [];
